$("#like_form_<%= @like.activity.id %>").html("<%= j render "likes/like",
  activity_id: @like.activity.id %>");
$("#like_count_<%= @like.activity.id %>").
  html("<%= pluralize @like.activity.likes.count, t("like") %>");
