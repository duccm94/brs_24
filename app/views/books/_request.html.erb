<%= form_for @request, remote: true do |f| %>
  <%= f.hidden_field :book_id, value: book_id %>
  <%= f.hidden_field :request_status, value: false %>
  <%= f.submit t(:request), class: "btn btn-primary" %>
  <%= render "shared/error_messages", object: f.object %>
<% end %>
